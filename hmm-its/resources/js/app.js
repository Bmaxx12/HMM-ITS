// HMM ITS — Public Website JS

// Navbar: handled by Alpine.js in navbar component

// Smooth reveal utility (for non-AOS elements if needed)
document.addEventListener('DOMContentLoaded', () => {
    // AOS is initialized inline in layout
    // Alpine.js is loaded via Filament CDN if available,
    // or can be imported here:
});
